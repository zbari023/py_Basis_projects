# documentation
file = open("test.txt","a")     # open file to read it
print(file.write("\n Bari "))              # print it
file.close()                    #  close it

# There are also "r+", which mean that you can read the file and write in there at the begin
# There are also "w" and "w+, which mean that you can write in the file and already written are clean
# There are also "a", which mean that you can read the file and write in there at the end
# by "a" when the file is not existing then it will creat a new file with the same name


# MODULES !!!!
# from py6_Funktion import *
# print(mysum(3, 7))
# from py_OOP import mobil
