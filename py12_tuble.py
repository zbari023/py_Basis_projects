tubleName=(1,2,3,4,7,6,11,9)
print(tubleName)               # (1, 2, 3, 4) kann kein Element verändert werden oder bearbeitet
print(len(tubleName))          # Anzahl der Elemente

print(max(tubleName))          # max
print(min(tubleName))          # min

